{
  "name": "callout",
  "private": true,
  "version": "0.1.0",
  "description": "Ferramenta privada de análise de desempenho e planejamento de estratégia.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web",
    "postinstall": "npm run build -w packages/shared",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w apps/api && npm run typecheck -w apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
